/** Generator id — the key the pipeline registers this entry under. */
export const generatorId = '@exp/gen-effect-schema'

/** The npm module every generated file imports `Schema` from. */
export const effectModule = 'effect'

/** The named export pulled from {@link effectModule}. */
export const schemaNamespace = 'Schema'
