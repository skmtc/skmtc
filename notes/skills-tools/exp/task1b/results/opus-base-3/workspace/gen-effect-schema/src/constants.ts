/** The generator id — also the key the pipeline registers this entry under. */
export const generatorId = '@exp/gen-effect-schema'

/** Module every generated file imports `Schema` from. */
export const effectModule = 'effect'
