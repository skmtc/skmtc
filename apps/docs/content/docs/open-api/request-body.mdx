---
title: OasRequestBody
description: OpenAPI request body
---

OasRequestBody is an object representing an OpenAPI request body.

## Properties

| Property | Type | Description |
| -------- | -------- | -------- |
| **oasType** | `'requestBody'` | Static value representing the OpenAPI request body type. |
| **description** | `string \| undefined` | Optional description of the request body. |
| **required** | `boolean \| undefined` | Optional flag marking the request body as required. |
| **content** | `Record<string, OasMediaType \| OasRef<'mediaType'>> \| undefined` | Optional map of request body representations by the media type. |
| **extensionFields** | `Record<string, unknown> \| undefined` | Optional map of custom fields defined by OpenAPI extensions. Keys are strings beginning with `x-` and values are of any valid JSON type. |

## Methods

### toSchema

Returns the `content` property for the given media type.

```js
requestBody.toSchema( mediaType = 'application/json' )
```

<details className="mt-5">
<summary>**Parameters**</summary>

| Parameter | Type | Description |
| --------- | -------- | -------- |
| mediaType | `string` | The media type to get the schema for. Defaults to `'application/json'`. |

</details>

**Return value**

An [OasSchema](/open-api/oas-schema) or [OasRef](/open-api/oas-ref) object.

### isRef

Returns `false` indicating that the request body is not a reference.

```js
requestBody.isRef()
```

### isRef

Returns `false` indicating that the request body is not a reference.

```js
requestBody.isRef()
```

**Return value**

`true`

### resolve

Returns the request body object itself.

```js
requestBody.resolve()
```

**Return value**

The request body object itself.

### resolveOnce

Returns the request body object itself.

```js
requestBody.resolveOnce()
```

**Return value**

The request body object itself.


