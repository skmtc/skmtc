---
title: ContentSettings
description: Settings object used to configure the output of a Transformer.
---

ContentSettings is an object that contains settings used to configure the output of a [Transformer](/transformer).

It can be obtained from a [GenerateContext](/generate-context) using either`context.toOperationContentSettings`, `context.toModelContentSettings` or `context.toGatewayContentSettings` depending on what you are doing.

## ContentSettings.empty

Create a new [ContentSettings](/content-settings) object using `identifier` and `exportPath`. `selected` will be `true` and `importFrom` will be set to `exportPath`.

```js
ContentSettings.empty({ identifier, exportPath })
```

<details className="mt-5">
<summary>**Parameters**</summary>

| Property        | Type                | Description  |
| --------------- |-------------------- | ------------ |
| **identifier**  | `Identifier`          | [Identifier](/identifier) to use in [Definition](/definition) with generated code. |
| **exportPath**  | `string`          | Path to the file where [Definition](/definition) with generated code should be registered. |

</details>

**Return value**

A new [ContentSettings](/content-settings) object with default values.

## Properties

| Property        | Type                | Description  |
| --------------- |-------------------- | ------------ |
| **isSelected**  | `boolean`           | Denotes where content is selected for generation. |
| **identifier**  | `Identifier`          | [Identifier](/identifier) to use in [Definition](/definition) with generated code. |
| **exportPath**  | `string`          | Path to the file where [Definition](/definition) with generated code should be registered. |
| **importFrom**  | `string`          | (Advanced) Path from which generated code should be imported from. Unless content is pinned, it will be same as `exportPath`. |