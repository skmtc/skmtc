{
  "id": "012-peer-not-in-config-map",
  "category": "cross-gen",
  "prompt": "My custom SKMTC generator composes with gen-typescript: in my Projection constructor I call this.insertNormalizedModel(TsProjection, { schema, fallbackName }). At generate time it throws: Cannot resolve language for generator '@skmtc/gen-typescript': not in the generator config map. A generator must be installed/configured to be inserted as a peer. The import resolves fine in my editor (I have @skmtc/gen-typescript in my generator's deno.json imports), so the package is clearly available. I'm thinking of catching the error and falling back to emitting the type inline as a string. What's going on?",
  "criteria": [
    {
      "id": "C1",
      "check": "Did the response identify that the peer generator must be installed/configured in the PROJECT (present in the run's generator config map), and that having the package in the authoring deno.json imports is not sufficient?"
    },
    {
      "id": "C2",
      "check": "Did the response direct the user to install/add the peer generator to the project (e.g. skmtc install @skmtc/gen-typescript <project>) rather than work around the error?"
    },
    {
      "id": "C3",
      "check": "Did the response reject catching the error and emitting the type inline as a string fallback?"
    }
  ],
  "scoring": "all_or_nothing",
  "expected_outcome": "edit"
}
