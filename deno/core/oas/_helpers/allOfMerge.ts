import type { OpenAPIV3 } from 'openapi-types'

export const getRef = (ref: OpenAPIV3.ReferenceObject): OpenAPIV3.SchemaObject => {
  // to be implemented later
}

export const allOfMerge = (schema: OpenAPIV3.SchemaObject) => {
  //
}
